# strings

## Table of Contents

- [Functions](#functions)
  - [padl](#padl)
  - [padr](#padr)

## Functions

### `padl`

```xylia
func padl(str: string, n: number, c: string) -> string
```

**Parameters:**

- `str` (`string`)
- `n` (`number`)
- `c` (`string`)

**Returns:** `string` 

### `padr`

```xylia
func padr(str: string, n: number, c: string) -> string
```

**Parameters:**

- `str` (`string`)
- `n` (`number`)
- `c` (`string`)

**Returns:** `string` 

